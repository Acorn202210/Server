<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.acorn2.FinalProject.qnaboard.dao.QnaBoardAnswerDao">

   <!-- 댓글을 저장할때 댓글의 글번호도 미리 QnaBoardAnswerDto 객체에 담아와야 한다. 
   <insert id="insert" parameterType="com.acorn2.FinalProject.qnaboard.dto.qnaBoardAnswerDto">
      INSERT INTO qna_board_answer
      (BOARD_COMMENT_NUM, BOARD_COMMENT_WRITER, content, BOARD_COMMENT_REF_GROUP, USER_REGDATE)
      VALUES(#{boardCommentNum}, #{boardCommentWriter}, #{content}, #{boardCommentRefGroup}, SYSDATE)
   </insert> -->
   
   <!-- 저장 예정인 댓글의 글번호를 미리 얻어내기 
   <select id="getSequence" resultType="int">
      SELECT qna_board_answer_seq.NEXTVAL
      FROM DUAL
   </select>-->
   
   <!-- 댓글 수정 
   <update id="update" parameterType="com.acorn2.FinalProject.qnaboard.dto.qnaBoardAnswerDto">
      UPDATE qna_board_answer
      SET content=#{content}, UPDATE_DATE=SYSDATE, UPDATE_ID=#{boardCommentWriter}
      WHERE BOARD_COMMENT_NUM=#{boardCommentNum}
   </update>-->
   
   <!-- 글 하나 가져오기
   <select id="getData" parameterType="int" resultType="com.acorn2.FinalProject.qnaboard.dto.qnaBoardAnswerDto">
      SELECT BOARD_COMMENT_NUM, BOARD_COMMENT_WRITER, content, BOARD_COMMENT_REF_GROUP, USER_REGDATE
      FROM qna_board_answer
      WHERE BOARD_COMMENT_NUM=#{boardCommentNum}
   </select> -->
      
   <!-- 댓글 삭제(DB Y로 변경) -->   
   
   
   <!-- 댓글 삭제(batch) -->
   
   
   <!-- -->
   
   
   <!-- -->
</mapper>